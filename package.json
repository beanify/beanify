{
  "name": "beanify",
  "version": "1.0.1",
  "description": "micro-services nats nats-micro-services",
  "main": "index.js",
  "scripts": {
    "test": "tap test/**/*.test.js --100 --cov --coverage-report=text",
    "unit": "tap test/**/*.test.js --100 --cov --coverage-report=lcov",
    "aaa": "tap test/**/beanify-router.test.js --100 --cov --coverage-report=text"
  },
  "author": "beanjs",
  "license": "MIT",
  "devDependencies": {
    "tap": "^14.9.1"
  },
  "typings": "./index.d.ts",
  "dependencies": {
    "ajv": "^6.10.2",
    "avvio": "^6.2.2",
    "beanify-plugin": "^1.0.2",
    "errio": "^1.2.2",
    "fastq": "^1.6.0",
    "nats": "^1.3.2",
    "nuid": "^1.1.0",
    "pino": "^5.13.5",
    "qlobber": "^3.1.0",
    "super-error": "^2.4.0"
  }
}
